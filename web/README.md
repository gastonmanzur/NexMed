# NexMed Web (Vite + React + TypeScript)

## Variables de entorno
1. Crear `web/.env.local` (o `web/.env`) a partir de `web/.env.example`.
2. Configurar `VITE_GOOGLE_CLIENT_ID=...` (debe comenzar con `VITE_`).
3. Reiniciar `npm run dev` después de cambiar cualquier variable de entorno.

> No subas claves reales al repositorio.

## Desarrollo
```bash
cd web
npm run dev
```
